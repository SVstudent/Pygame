# Aegis webhook test bed

Self-contained Python project used to exercise the Aegis automatic PR
verification workflow. `mathkit` holds small stats helpers with a
pytest suite. Everything lives on the `aegis-test-*` branches so the
repo's main content is untouched.
